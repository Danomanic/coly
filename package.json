{
  "name": "coly",
  "version": "1.0.0",
  "private": true,
  "description": "Coly — landing page for a global container hosting platform (PaaS).",
  "scripts": {
    "generate-map": "node scripts/generate-map.js",
    "build": "node scripts/build-site.js",
    "start": "npm run build && node scripts/serve.js",
    "serve": "node scripts/serve.js"
  },
  "engines": {
    "node": ">=18.0"
  }
}
